//然后新建仓库和ssh

git config --global user.name "chenyomi"
git config --global user.email "408550179@qq.com"

生成公匙
ssh-keygen -t rsa -C "408550179@qq.com"

//验证
ssh -T git@github.com


上传 从这开始

git init(初始化这个库)
git add .    (将文本上传到本地git库)
git commit -m "first"

选择http
git remote add origin https://github.com/chenyomi/habit.git

或者选择ssh
git remote add origin git@github.com:chenyomi/habit.git  
最后上传本地文件

git push -u origin master (将本地文件上传到gitHub中)

